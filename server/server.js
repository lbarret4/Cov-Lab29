const express = require('express');
const path = require('path');
const cors = require('cors');
const PORT = 3000;

let app = express();

app.use(cors());
app.use(express.json());










app.listen(PORT);




